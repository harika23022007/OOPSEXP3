2.

package haa;
import java.util.Scanner;
public class essemble {
       public static void main(String[] args) {
              Scanner sc = new Scanner(System.in);
              System.out.print("Enter a string: ");
              String input = sc.nextLine();
              char firstChar = input.charAt(0);
              String result = firstChar + input.substring(1).replace(firstChar, '$');
              System.out.println("Output: " + result);
              sc.close();
       }
}

3.

package haa;
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
public class essemble {
       public static void main(String[] args) {
              int spaces = 0, words = 0, characters = 0, lines = 0;
                      String line;
                      System.out.println("Enter your text (end with an empty line):");
                       while ((line = reader.readLine()) != null && line.length() > 0) {
                               lines++;
                               characters += line.length(); // count all characters except newline
                               spaces += countSpaces(line);
                               words += countWords(line);
                       }
                } catch (IOException e) {
                    System.err.println("Error reading input: " + e.getMessage());
}

